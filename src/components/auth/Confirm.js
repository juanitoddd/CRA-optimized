import React from "react";
import styled from "styled-components";

export const Confirm = () => (
  <div>  
    <h1>You have mail !, check your Email to confirm your account.</h1>
  </div>
);

export default Confirm;
